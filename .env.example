# Example environment variables for local development
# Copy this file to `.env.local` and fill in real values. Do NOT commit `.env.local` to source control.

# (Optional) Server-side Gemini API key. Some setups use this; prefer service account ADC for server-side auth.
GEMINI_API_KEY=your_gemini_api_key_here

# Recommended: use a Google service account JSON for server-side authentication (Application Default Credentials)
# Point this variable at the absolute path to your downloaded JSON key file. Example for Windows:
# GOOGLE_APPLICATION_CREDENTIALS=D:/KBSA/keys/gcloud-sa.json
GOOGLE_APPLICATION_CREDENTIALS=D:/path/to/your/service-account-key.json

# Optional: set a custom server port
# PORT=4000
